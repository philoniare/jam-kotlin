package io.forge.jam.core

import kotlin.test.Test

class BlockEncodingTest {

    @Test
    fun testEncodeBlock() {
//        // Paths to your JSON and binary files
//        val jsonFilePath = "src/test/resources/block.json"
//        val binFilePath = "src/test/resources/block.bin"
//
//        // Read JSON data from file
//        val jsonData = Files.readString(Paths.get(jsonFilePath))
//
//        // Parse the JSON data
//        val json = Json { ignoreUnknownKeys = true }
//        val block = json.decodeFromString<Block>(jsonData)
//
//        // Encode the block
//        val encodedBlock = block.encode()
//
//        // Read expected binary data from file
//        val expectedOutputBytes = Files.readAllBytes(Paths.get(binFilePath))
//
//        // Compare the encoded bytes with the expected output bytes
//        assertContentEquals(
//            expectedOutputBytes,
//            encodedBlock,
//            "Encoded bytes do not match expected output"
//        )
    }
}
